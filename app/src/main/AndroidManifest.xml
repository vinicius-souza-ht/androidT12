<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.fabricadeprogramador.fdpapp1">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MenuActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ResultadoCalculadoraActivity">
        </activity>
        <activity android:name=".ResultadoConversaoActivity">
        </activity>
        <activity android:name=".CalculadoraActivity">
        </activity>
        <activity android:name=".ConversorActivity">
        </activity>
        <activity android:name=".AgendaActivity">
        </activity>
        <activity android:name=".ListaAgendaActivity">
        </activity>
    </application>

</manifest>